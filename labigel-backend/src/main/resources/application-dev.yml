spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/labigel_db
    username: labigel
    password: labigel123
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.springframework.security: DEBUG
    com.labigel.backend: DEBUG
